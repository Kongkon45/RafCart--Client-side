import LogoutButton from '@/components/auth/logout';
import React from 'react';

const Logout = () => {
    return (
        <div>
            <LogoutButton/>
        </div>
    );
};

export default Logout;